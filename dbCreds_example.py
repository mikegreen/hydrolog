#!/usr/bin/python

# this is dbCreds

import MySQLdb as mdb

# Example
# con = mdb.connect('mysql.yourserver.com', 'username', 'password', 'database_name');
con = mdb.connect('mysql.yourserver.com', 'username', 'password', 'database_name');